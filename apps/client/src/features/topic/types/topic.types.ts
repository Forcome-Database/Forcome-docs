export interface ITopic {
  id: string;
  name: string;
  description?: string;
  icon?: string;
  slug: string;
  position: string;
  directoryId: string;
  spaceId: string;
  workspaceId: string;
  creatorId: string;
  createdAt: string;
  updatedAt: string;
}
