export interface IDirectory {
  id: string;
  name: string;
  description?: string;
  icon?: string;
  slug: string;
  position: string;
  spaceId: string;
  workspaceId: string;
  creatorId: string;
  createdAt: string;
  updatedAt: string;
}
