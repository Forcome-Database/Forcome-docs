# 任务计划

## 目标
审核后端 AI 创建、代理编排和文档处理流程：
- `agent-service/app/**/*`
- `apps/server/src/ee/ai/**/*`
- 相关测试和实施支持的文档

答：
1.当前`run`/`resume`/`commit`/`optimize`流量
2. `selection edit`、`document transform`和`blank-page create`是否共享一个编排器
3. 哪些实现可能会导致章节不连续、PDF/DOC 优化输出损坏或重新优化时数据/图像丢失
4. 哪些模块应该保留，哪些模块应该退役

请勿修改产品代码。

## 阶段
| 阶段 | 状态 | 说明 |
|---|---|---|
| 1. 映射服务器端入口点 | 已完成 | 控制器、服务、网关、DTO、测试 |
| 2. 映射代理服务运行时 | 已完成 | Orchestrator、会话存储、工具、工作人员、持久性 |
| 3. 交叉检查测试和文档 | 已完成 | 只有实现支持的文档才算数 |
| 4. 生成结构化摘要 | 进行中 | 文件路径、函数名称、保留/停用建议 |

## 遇到的问题
| 问题 | 尝试 | 处理方式 |
|---|---|---|
| `Get-Content` 在低超时的计划文件上超时 | 1 | 以更长的超时时间和 `-First` 重新运行 |
